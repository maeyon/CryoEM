%間隔eごとのプロット ()
%始点からの曲線上の距離を不定積分で求める


%eの間隔を決める
e = l / 100;

%間隔eごとにプロット
for i = 0:100
    for j = 0:100
        for k = 0:10000
            if u(k) == j*e
                mu(i) = [X(k), Y(k)];
            end
        end
    end
end
