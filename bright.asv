%輝度値の推定
%隣接する4つの画素を見つける
nu(i,j)

%輝度値の推定
g = ;
for i = 0:100
    for j = 1 : 4
        d(i,j) = norm(mu(i) - nu(i,j));
        w(i, j) = exp(- g*d(i, j)^2) / symsum(exp(-g*d(i, k)), k, 1, 4);
    end
    b(i) = symsum(k^2, k, [0 10])
end